# FastAPI and core dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Database and ORM
sqlmodel>=0.0.14
psycopg2-binary>=2.9.7
alembic>=1.12.0

# Authentication and security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
jwcrypto>=1.5.0

# Configuration and environment
pydantic[email]>=2.4.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Payment processing
stripe>=7.0.0

# HTTP client for external APIs
httpx>=0.25.0

# Logging and monitoring
structlog>=23.2.0
python-json-logger>=2.0.0

# Production server
gunicorn>=21.2.0

# WebSocket support for real-time tracking
python-socketio[asyncio]>=5.10.0

# Rate limiting
slowapi>=0.1.9

# AWS SDK for Cognito admin operations
boto3>=1.28.0

# Development dependencies (optional but recommended for better file watching)
watchfiles>=0.21.0

# Background task scheduling (optional - for cleanup jobs)
apscheduler>=3.10.0